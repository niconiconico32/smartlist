/**
 * App Configuration
 */

// Developer Mode - Automatically true in development, false in production builds
export const DEV_MODE = __DEV__;

// Enable test buttons and debug features
export const SHOW_TEST_BUTTONS = DEV_MODE;

// Enable console logs
export const ENABLE_LOGGING = DEV_MODE;

// Other config
export const APP_VERSION = '1.0.0';
